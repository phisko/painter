#pragma once

namespace rotate_sun {
	//! putils reflect all
	//! class_name: rotate_sun_config
	//! metadata: [("config", true)]
	struct config {
		float sun_rotation = .1f;
	};
}

#include "config.rpp"